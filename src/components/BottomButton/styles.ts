import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    paddingHorizontal: 24,
    marginBottom: 48,
  },
  withAbsolute: {
    position: 'absolute',
    bottom: 0,
  },
  withoutAbsolute: {
    position: 'relative',
  },
});
